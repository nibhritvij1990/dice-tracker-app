import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nibhritvij.dicetracker',
  appName: 'dice-tracker-app',
  webDir: 'dist'
};

export default config;
